spring:
  application:
    name: aiworkflow

  datasource:
    url: jdbc:postgresql://localhost:5432/aiworkflow
    username: aiworkflow
    password: aiworkflow

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

  flyway:
    enabled: true
    repair-on-migrate: true
    validate-on-migrate: false

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics

openai:
  api-key: ${OPENAI_API_KEY:}
  embedding-model: text-embedding-3-small
  chat-model: gpt-4.1-mini

security:
  jwt:
    secret: ${JWT_SECRET:portfolio-demo-secret-change-me-please}
    expiration-seconds: 86400
